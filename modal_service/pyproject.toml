[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "modal_service"
version = "0.1.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".."]
